{
  "name": "bookcart-mern-ecommerce",
  "version": "1.0.0",
  "description": "BookCart — MERN Online Bookstore Full-Stack Web Application",
  "scripts": {
    "server-install": "npm install --prefix server",
    "client-install": "npm install --prefix client",
    "install-all": "npm install --prefix server && npm install --prefix client",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "seed": "npm run seed --prefix server",
    "build": "cd client && npm install --include=dev && npm run build",
    "vercel-build": "cd client && npm install --include=dev && npm run build"
  },
  "keywords": [
    "mern",
    "react",
    "node",
    "express",
    "mongodb",
    "redux-toolkit",
    "bookstore"
  ],
  "author": "Pratham Shahi",
  "license": "MIT"
}
